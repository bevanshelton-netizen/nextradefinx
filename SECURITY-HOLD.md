# NexTradeFinX security hold

Repository setup detected before production seed upload.

**Do not add secrets here.**

This repository should be changed to **Private** before the V23 controlled-beta seed is populated.

Allowed public/client configuration later:
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY

Never commit:
- SUPABASE_SERVICE_ROLE_KEY
- database passwords
- broker secrets
- live trading keys
- OTPs or user passwords
